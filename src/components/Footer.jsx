import styles from "./Footer.module.css";

const Footer = () => {
  return (
    <footer className={styles.footer}>
      <p>Created for the CTD Jay React class</p>
    </footer>
  );
};

export default Footer;
